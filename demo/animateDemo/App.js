/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import React, { Component } from 'react';
import {
  Platform,
  StyleSheet,
  Text,
  View
} from 'react-native';
import {CustomAnimation} from 'react-native-tabbar-animated'

const instructions = Platform.select({
  ios: 'Press Cmd+R to reload,\n' +
    'Cmd+D or shake for dev menu',
  android: 'Double tap R on your keyboard to reload,\n' +
    'Shake or press menu button for dev menu',
});

export default class App extends Component<{}> {
  render() {
    return (
      <View style={styles.container}>
        {/*旋转*/}
        <CustomAnimation animationStyle={'spin'} imageStyle={{width:30,height:30}} spinTime={4000}/>
        {/*放大缩小*/}
        <CustomAnimation animationStyle={'spring'} imageStyle={{width:30,height:30}} junpTime={2000}/>
        {/*移动*/}
        <CustomAnimation animationStyle={'linear'} linearTime={3000}/>
      </View>
    );
  }
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#F5FCFF',
  },
  welcome: {
    fontSize: 20,
    textAlign: 'center',
    margin: 10,
  },
  instructions: {
    textAlign: 'center',
    color: '#333333',
    marginBottom: 5,
  },
});
